# GitHub Trending 深度分析报告系统

这是一个自动化系统，用于每天获取 GitHub Trending 数据并生成深度分析报告。

## 功能特性

- **自动数据获取**: 每天自动抓取 GitHub Trending 页面
- **深度分析报告**: 对每个热门仓库进行技术分析和市场前景评估
- **Markdown 格式**: 生成易读的 Markdown 报告
- **定时任务**: 支持 cron 定时执行

## 文件结构

```
.
├── README.md                     # 本文件
├── fetch_trending.py            # GitHub Trending 数据获取器
├── generate_report.py           # 报告生成器核心
├── run_report.sh                # 主运行脚本
├── setup_cron.sh                # Cron 定时任务设置脚本
└── github-trending-report-*.md  # 生成的报告文件
```

## 快速开始

### 1. 安装依赖

确保系统已安装 Python 3.8+：

```bash
python3 --version
```

### 2. 手动运行一次

```bash
cd /Users/bytedance/github-trending-report
./run_report.sh
```

这会：
1. 获取当天的 GitHub Trending 数据
2. 生成深度分析报告
3. 保存为 Markdown 文件

### 3. 设置定时任务（每天9点执行）

```bash
./setup_cron.sh
```

这会：
- 检查必要的文件和权限
- 创建 cron 任务（每天 9:00 运行）
- 显示设置结果

验证 cron 任务：

```bash
crontab -l
```

## 报告内容

生成的报告包含：

1. **执行摘要**: 热度概览、技术领域分布
2. **深度分析**: 每个热门仓库的详细分析
   - 项目简介和技术原理
   - 核心特性
   - 潜在应用方向
   - 市场前景
3. **趋势总结**: 技术趋势洞察和投资建议

## 手动触发报告生成

如果你想在定时任务之外手动生成报告：

```bash
cd /Users/bytedance/github-trending-report
./run_report.sh
```

## 查看历史报告

所有报告都保存在项目目录中，按日期命名：

```bash
ls -la *.md
```

查看最新报告：

```bash
cat github-trending-report-$(date +%Y-%m-%d).md
```

## 故障排除

### Cron 任务没有运行

1. 检查 cron 服务状态：
   ```bash
   sudo launchctl list | grep cron
   ```

2. 检查日志文件：
   ```bash
   tail -f /Users/bytedance/github-trending-report/cron.log
   ```

3. 手动运行测试：
   ```bash
   /bin/bash /Users/bytedance/github-trending-report/run_report.sh
   ```

### 数据获取失败

GitHub 可能会限制频繁的请求。如果遇到问题：

1. 检查网络连接
2. 稍后重试
3. 检查 GitHub 是否可访问

## 技术说明

### 数据获取

- 使用 `requests` 库获取 GitHub Trending 页面
- 使用正则表达式解析 HTML 提取仓库信息
- 支持处理今日新增 stars、总 stars、语言等数据

### 报告生成

- 纯 Python 实现，无外部依赖
- 生成标准的 Markdown 格式
- 包含目录结构和锚点链接

## 贡献

欢迎提交 Issue 和 PR！

## 许可证

MIT License

## 联系方式

如有问题或建议，请通过 GitHub Issue 联系。

---

*Happy Analyzing! 🚀*
